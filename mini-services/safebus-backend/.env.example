# SafeBus reference backend — local environment (never commit real .env files)
# JWT signing secret. REQUIRED (>= 32 chars) when NODE_ENV=production.
# Generate: openssl rand -hex 32
JWT_SECRET=safebus-sandbox-8f4c1d6e2a9b7f0c3d5e8a1b4c7f0e2d9a6b3c8f1e4d7a0b5c2e9f6d3a8b1c4

# Pin REST + Socket.IO CORS to the deployment origin(s) in production
# (comma-separated). Unset = permissive (sandbox only).
# ALLOWED_ORIGIN=https://your-domain.example
